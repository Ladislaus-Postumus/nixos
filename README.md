# My NixOS Setup

This is a very basic nixos setup containing only necessery tools like a good looking shell and neovim.

## Update after changes

1. sudo nix flake update — updates the lock-file
1. sudo nixos-rebuild switch --flake . — updates the system based on lock-file
